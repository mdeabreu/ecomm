import { RequiredDataFromCollectionSlug } from 'payload'

export const imageGalaxyBlackData: RequiredDataFromCollectionSlug<'media'> = {
  alt: 'Galaxy Black PLA+ spool with subtle glitter finish',
}
